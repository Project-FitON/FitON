import 'package:flutter/material.dart';

class AddDependentsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add Dependent'),
        backgroundColor: Colors.deepPurple,
      ),
      body: Center(
        child: Text('Add Dependents Screen Placeholder'),
      ),
    );
  }
}
